;(function () {

    'use strict';

    angular
        .module('factory.url', [])
        .factory('url', url);

    url.$inject = [];

    function url() {

        var baseUrl = 'https://ico.grassbusinesslabs.tk/api/web/v1/';

        return {

        };
    }
})();