;(function () {
    'use strict';

    angular.module('service.helpSvc', []).factory('helpSvc', helpSvc);

    helpSvc.$inject = [];

    function helpSvc() {
        var model = {};
        return model;
    }
})();