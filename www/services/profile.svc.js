;(function () {
    'use strict';

    angular.module('service.profileSvc', []).factory('profileSvc', profileSvc);

    profileSvc.$inject = [];

    function profileSvc() {
        var model = {};
        return model;
    }
})();