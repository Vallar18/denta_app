;(function () {
    'use strict';

    angular.module('service.patientSvc', []).factory('patientSvc', patientSvc);

    patientSvc.$inject = [];

    function patientSvc() {
        var model = {
        };
        return model;

    }
})();