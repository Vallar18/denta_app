;(function () {
    'use strict';

    angular.module('service.authSvc', []).factory('authSvc', authSvc);

    authSvc.$inject = [];

    function authSvc() {
        var model = {};
        return model;
    }
})();