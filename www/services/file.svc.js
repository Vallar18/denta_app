;(function () {
    'use strict';

    angular.module('service.fileSvc', []).factory('fileSvc', fileSvc);

    fileSvc.$inject = [];

    function fileSvc() {
        var model = {};
        return model;
    }
})();